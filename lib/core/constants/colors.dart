import 'package:flutter/material.dart';
import 'dart:ui';
//
//
class AppColors {
//
//   // Primary Colors..
// User interact and associate with your app most(Elevated Buttons,AppBar,Progress Indicator.etc ).
//
    static const Color primary = Color(0xFF4169E1);
    static  const Color primaryLight =Color(0xFF808080);
  static const Color primaryDark = Color(0xFF0D3A55);

  // Secondary Colors..

    static const Color accent = Color(0xFFFFA726); // Amber 400


//   // BackGround Colors..
static const Color background = Color(0xFFFFFFFF);
 static const Color gradientStart = Color(0xFF13547A);
static const Color gradientEnd = Color(0xFF80D0C7);


 // Text Colors..

// For dark text on light background (most common)
static const Color text = Color(0xFF212121); // near-black
 static const Color textPrimary = Color(0xFF212121); // main body text
 static const Color textSecondary = Color(0xFF757575); // subtitles / hints



//   // Error/Success  Colors..

 static const Color error = Color(0xFFB71C1C);
 static const Color success =Color(0xFF2E7D32);



}
